package com.origin.repository;

import com.origin.entity.RaidTemplate;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RaidTemplateRepository extends JpaRepository<RaidTemplate, Long> {
}
