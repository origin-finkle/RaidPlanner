package com.origin.repository;

import com.origin.entity.AutoComposeSettings;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AutoComposeSettingsRepository extends JpaRepository<AutoComposeSettings, Long> {
}
